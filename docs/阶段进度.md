# 后端阶段进度

## 当前状态

严格按计划计，当前处于：

- `01_项目骨架与交付基线`：已收口。
- `02_多格式文件解析阶段计划`：准备开始，已有 converted_textbooks 导入 MVP。
- `03_知识抽取与图谱阶段计划`：未开始。

## 计划 01 收口内容

- FastAPI 后端可启动。
- React/Vite 前端最小工作台可启动，供接口联调用。
- 已建立 `backend/`、`frontend/`、`docs/`、`report/`、`data/` 目录。
- 已提供 `.env.example`、`.gitignore`、`backend/requirements.txt`、`frontend/package.json`。
- `GET /api/health` 可用。
- `POST /api/textbooks/upload` mock 导入可用。
- `GET /api/jobs/{job_id}` mock 任务状态可用。
- 数据契约已提前落地到后端 Pydantic、前端 TypeScript 和文档。

## 计划 02 边界

计划 02 只处理：

- 原始输入文件识别。
- 文档解析和清洗。
- 统一输出 `RawFile -> DocumentElement -> Section -> Chunk`。
- `source_locator` 证据链。

计划 02 不处理：

- KnowledgeNode / KnowledgeEdge 抽取。
- KG 构建。
- GraphRAG。
- 跨教材整合压缩。

## 下一步

1. 把解析服务从 converted_textbooks 专项导入器中抽象出来。
2. 让上传接口根据文件类型选择解析路径。
3. 先支持 `txt/md` 和 converted_textbooks。
4. 再补 PDF / DOCX / XLSX / PPTX 的最小解析入口。

## 计划 02 当前小步

- `converted_textbooks`：保留为教材演示导入入口。
- `txt/md`：上传后进入统一解析层，输出 `RawFile -> DocumentElement -> Section -> Chunk`。
- 其他格式：暂时返回明确错误，等对应解析器接入。
