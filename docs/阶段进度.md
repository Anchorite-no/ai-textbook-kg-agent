# 后端阶段进度

## 当前状态

严格按计划计，当前处于：

- 00 路线图 `阶段 0：项目骨架`：已收口。
- 00 路线图 `阶段 1：数据契约先行`：已收口。
- 00 路线图 `阶段 2：多格式解析到统一 JSON`：后端已基本收口。
- 00 路线图 `阶段 3：异步任务 DAG`：基本完成，已完成异步解析最小闭环。
- 00 路线图 `阶段 4：RAG 证据索引`：基本完成，已完成本地混合 Evidence Index 和 20 题 benchmark。
- 00 路线图 `阶段 5：知识点抽取`：基本完成，已完成单本教材 schema-guided 节点/边抽取。
- 00 路线图 `阶段 6：多层 KG 构建`：基本完成，已完成 Document Tree / Concept KG / Evidence Graph 分层输出。

## 计划 01 收口内容

- FastAPI 后端可启动。
- React/Vite 前端最小工作台可启动，供接口联调用。
- 已建立 `backend/`、`frontend/`、`docs/`、`report/`、`data/` 目录。
- 已提供 `.env.example`、`.gitignore`、`backend/requirements.txt`、`frontend/package.json`。
- `GET /api/health` 可用。
- `POST /api/textbooks/upload` mock 导入可用。
- `GET /api/jobs/{job_id}` mock 任务状态可用。
- 数据契约已提前落地到后端 Pydantic、前端 TypeScript 和文档。

## 计划 02 边界

计划 02 只处理：

- 原始输入文件识别。
- 文档解析和清洗。
- 统一输出 `RawFile -> DocumentElement -> Section -> Chunk`。
- `source_locator` 证据链。

计划 02 不处理：

- KnowledgeNode / KnowledgeEdge 抽取。
- KG 构建。
- GraphRAG。
- 跨教材整合压缩。

## 计划 02 已完成内容

- `converted_textbooks`：保留为教材演示导入入口，并已做最小正文结构清洗。
- `txt/md/markdown`：上传后进入统一解析层，按标题或固定窗口切分，保留行号。
- `pdf`：逐页抽取文本，过滤高频页眉页脚；优先识别章节，失败时按 5 页窗口切分。
- `docx`：按 Heading 分 section，表格按原位置转 Markdown 表格，保留段落范围。
- `xlsx/csv/tsv`：识别表头，跳过空行列，按 40 行窗口切分，保留 sheet/行范围。
- `pptx`：每页 slide 一个 section，按 top/left 合并文本框，抽取备注和表格。
- `.doc/.xls/.ppt`：暂不直接解析，返回清晰错误；后续通过 LibreOffice 转换兜底。
- `/api/textbooks/upload`：兼容单文件上传和无文件 demo 导入。
- `/api/textbooks/upload-batch`：批量上传，单文件失败不会阻断其它文件。
- `/api/textbooks/{raw_file_id}/parse`：对已保存上传文件重新解析。
- `/api/uploads/sessions`：大文件分片上传会话，支持断点式补传、sha256 合并校验和完成后解析。
- `/api/textbooks/upload-async`：上传后立即返回异步解析 job。
- `/api/textbooks/{raw_file_id}/parse-async`：对已保存上传文件启动后台重解析。
- `/api/uploads/sessions/{session_id}/complete-async`：大文件后台合并并解析。
- `/api/jobs/{job_id}/retry`：重试带上下文的失败异步任务。
- `/api/rag/index`：建立 chunk 证据索引。
- `/api/rag/status`：查看索引状态。
- `/api/rag/query`：返回带 citations 和 source_chunks 的证据问答。
- `backend/scripts/smoke_phase2.py`：覆盖解析器和 API 烟测。
- `backend/scripts/smoke_00_stage3_async.py`：覆盖异步 DAG、失败重试和分片 complete-async。
- `backend/scripts/smoke_00_stage4_rag.py`：覆盖索引、查询、引用和无答案拒答。
- `backend/scripts/export_openapi.py`：导出 `docs/openapi.snapshot.json` 供前端 codegen。
- 全局错误响应已统一为 `{ message, code, detail }`。

## 阶段 2-6 验证命令

```powershell
cd D:\Hackathon
$env:PYTHONPATH=(Resolve-Path backend).Path
python -m compileall backend\app
backend\.venv\Scripts\python.exe backend\scripts\smoke_phase2.py
backend\.venv\Scripts\python.exe backend\scripts\smoke_00_stage3_async.py
backend\.venv\Scripts\python.exe backend\scripts\smoke_00_stage4_rag.py
backend\.venv\Scripts\python.exe backend\scripts\smoke_phase3.py
backend\.venv\Scripts\python.exe backend\scripts\smoke_00_stage6_layered_kg.py
backend\.venv\Scripts\python.exe backend\scripts\export_openapi.py
```

烟测覆盖：`txt/md/csv/tsv/xlsx/docx/pptx/pdf` 解析、`source_locator` 格式字段、单文件上传、批量上传部分失败、大文件分片上传、任务查询和重新解析。

异步 DAG 当前覆盖：

```text
assemble_file -> detect_format -> parse_elements -> build_sections -> chunk_sections -> persist_parsed
```

阶段 4 当前是本地混合 Evidence Index：BM25 + lightweight hash embedding + query coverage，尚未接模型 embedding / FAISS / Chroma。它已经满足 chunk 证据索引、top-k 检索、citations、source_chunks、无答案拒答和基础 benchmark；向量库属于后续增强。

## 00 阶段 5 已完成内容

- `POST /api/graph/build`：按单本教材 `raw_file_id` 构建 `GraphResponse`。
- `GET /api/graph?raw_file_id={id}&top_n=200`：读取单本教材图谱。
- `GET /api/graph/nodes/{node_id}`：读取节点详情、相邻边和证据 chunk。
- `backend/app/services/llm_client.py`：OpenAI-compatible LLM 调用骨架。
- `backend/app/services/llm_cache.py`：按 prompt/content hash 缓存 LLM 输出。
- `backend/app/services/knowledge_graph_builder.py`：按 section 生成节点和关系；未配置 LLM 时走确定性兜底抽取。
- LLM 输出支持 JSON fence / 前后噪声容错解析。
- 兜底关系覆盖 `CONTAINS`、`PREREQUISITE_OF`、`APPLIES_TO`、`CAUSES`、`LEADS_TO`、`EXPLAINS`、`PARALLEL_WITH`。
- `backend/scripts/smoke_phase3.py`：覆盖构建、读取、节点详情、缓存命中、多关系和证据绑定。
- 节点/边 metadata 写入 `source_quote_verified` 和 `evidence_strategy`，用于检查 LLM/兜底证据是否能落回原文。
- 关系映射覆盖 `IS_A`、`PART_OF`、`CONTRASTS_WITH`。

计划 03 当前仍不处理：

- 跨教材整合压缩。
- RAG / GraphRAG。
- 教师编辑。

## 00 阶段 6 已完成内容

- `POST /api/kg/layers/build`：为单本教材构建多层 KG。
- `GET /api/kg/layers?raw_file_id={id}`：读取多层 KG。
- `LayeredGraphResponse`：内部保留 `document_tree`、`concept_kg`、`evidence_graph`、`alias_alignment`、`integration_decision`、`teacher_edit`、`graphrag_retrieval`。
- `document_tree`：连接 `RawFile -> Section -> DocumentElement / Chunk`，包含 `CONTAINS`、`HAS_ELEMENT`、`HAS_CHUNK`、`NEXT`。
- `concept_kg`：复用阶段 5 的知识点节点和语义关系边。
- `document_tree`：顺序边同时包含 `NEXT` 和 `PREVIOUS`。
- `evidence_graph`：把知识节点、知识关系、SourceQuote 和 chunk 证据显式连接，确保节点和边可回到 `source_locator`。
- `alias_alignment`、`integration_decision`、`teacher_edit`、`graphrag_retrieval`：按 00 阶段边界保留为 `reserved`，不提前生成跨教材对齐、压缩决策、GraphRAG 或教师编辑结果。
- `backend/scripts/smoke_00_stage6_layered_kg.py`：覆盖构建、读取、缓存命中、分层状态、证据边和 source locator。

阶段 6 当前仍不处理：

- 跨教材术语对齐与去重。
- 整合压缩和 merge/keep/remove 决策。
- GraphRAG 问答路径检索。
- 教师编辑事件。

## 下一步

进入 00 路线图 `阶段 7：术语对齐与去重`，先做同义词/别名候选和跨教材候选对齐，不直接删除原始节点。

## 审计文档

阶段 4-6 的问题清单对齐见：`docs/阶段4-6核心审计.md`。
