# 测试与运行报告

生成日期：2026-05-10

## 1. 验证命令

前端类型检查：

```powershell
cd D:\Hackathon\frontend
npm run typecheck
```

结果：通过。

前端生产构建：

```powershell
cd D:\Hackathon\frontend
npm run build
```

结果：通过。Vite 仅提示 bundle 大小超过 500 kB，非阻断问题。

后端语法检查：

```powershell
cd D:\Hackathon
$env:PYTHONPATH='backend'
backend\.venv\Scripts\python.exe -m compileall backend\app
```

结果：通过。

OpenAPI 导出：

```powershell
cd D:\Hackathon
$env:PYTHONPATH='backend'
backend\.venv\Scripts\python.exe backend\scripts\export_openapi.py
```

结果：`docs/openapi.snapshot.json` 已更新。

前端契约同步：

```powershell
cd D:\Hackathon\frontend
npm run sync:api:file
```

结果：`frontend/src/types/api.generated.ts` 已更新。

## 2. 七本书全量重跑

执行脚本：

```powershell
cd D:\Hackathon
backend\.venv\Scripts\python.exe backend\scripts\rebuild_seven_books_full.py --max-sections 3000 --max-nodes-per-section 12
```

参数说明：

- `force_rebuild=True`
- `build_graph=True`
- `build_layered_graph=True`
- `build_rag=True`
- `build_alignment=True`
- `build_integration=True`
- `use_llm=False`
- `alignment_max_nodes=4000`
- `integration_max_nodes=4000`

结果：

| 项 | 值 |
| --- | ---: |
| 数据集状态 | ready |
| 教材 | 7 |
| Section | 2,014 |
| Chunk | 5,782 |
| KG 节点 | 16,585 |
| KG 边 | 45,800 |
| RAG chunk | 5,782 |
| GraphRAG graph_count | 7 |

## 3. 图谱质量

| 指标 | 值 |
| --- | ---: |
| 节点总数 | 16,585 |
| 边总数 | 45,800 |
| 孤立节点 | 603 |
| 孤立节点率 | 3.64% |
| 上下文弱链接 | 28,395 |

本次优化后，图谱不再依赖纯强语义边。系统会保留同章节、同 chunk、相邻章节的弱链接，并在 metadata 标记其用途，降低前端展示时的零散节点。

## 4. RAG / GraphRAG 状态

RAG：

- `status=ready`
- `textbook_count=7`
- `chunk_count=5782`
- `embedding_backend=local_hash_embedding`
- `embedding_dimensions=256`
- `source_locator` 覆盖率 100%

GraphRAG：

- `status=ready`
- `graph_count=7`
- `node_count=16585`
- `edge_count=45800`
- `alignment_available=true`
- `integration_available=true`

## 5. Integration 状态

| 指标 | 值 |
| --- | ---: |
| original_char_count | 3,023,632 |
| retained_char_count | 225,283 |
| original_node_count | 4,000 |
| integrated_node_count | 3,931 |
| merged_node_count | 30 |
| kept_node_count | 3,876 |
| refined_node_count | 39 |
| conflict_count | 4 |
| compression_ratio | 7.45% |
| target_compression_ratio | 30% |
| evidence_coverage_ratio | 100% |

## 6. GitHub 状态

仓库：

```text
https://github.com/Anchorite-no/ai-textbook-kg-agent
```

当前可见性：public。

最新提交以 GitHub `main` 分支为准；本文档随交付状态同步维护。

## 7. 注意事项

- `materials/`、`plans/` 仍是本地未跟踪目录，没有提交到 GitHub。
- `.env`、`.env.development` 不提交，LLM key 不进入仓库。
- 前端已把左侧章节树限制在下半区域滚动展示，避免长目录撑爆布局。
